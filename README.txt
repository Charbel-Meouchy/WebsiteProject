Personal Portfolio Page
Charbel Meouchy

To do this homework I used the old About Me page we did for the last homework and designed it.
I first started by moving the nav section to header and designed it with a blue background color, removing the lines underneath the links and positioning them, which I did the same for the footer as well so the design can match.
I added a hero section and a welcome sentence at the begining.
Added my skills using a no number list and designed it as well, which I added a link to it in the nav section .
Made a folder images that I put my image in and a css folder for the style.css file to match the assignment requirements.
I set box-sizing: border-box for the entire website to get the right dimensions, so now the number of pixels set includes the padding and border too.
Set a body typography as well as a consistent sizing, weight, line-height for all headers and paragraphs.
Assignment requirements checklist:

Semantic HTML only (header, nav, main, section, footer) ,no divs used in the structural elements just used it in the contact form where i used it to group each label alone not for page structure.
All four required sections included: Hero/Intro, About Me, Skills, Contact and i added two extra sections which are: (My Studies and My Goals).
Custom external stylesheet css/style.css no CSS framework used.
One image included with meaningful alt text
Contact form with labeled name, email, and message fields.
Consistent color palette, font, and spacing applied across the whole page.
No inline styles anywhere, no unused CSS rules.
File structure matches what was asked: index.html, css/style.css, images/.
No JavaScript used, as the assignment specifies.
Code is consistently indented with no commented-out dead code.(Every detail here matter nothing is extra/not needed in my code)